

const JSXRules = () => {
  return (
    <div>
    <h1>JSX Must return single Parent Element</h1>
    <h2>All tags Must be Closed </h2>
    <h3>For declaring Class used ClassName=""</h3>
    <h4>when label declared ,we see a for in html where as in jSX we need to declare it as htmlFor</h4>
    </div>
  )
}

export default JSXRules
